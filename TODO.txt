Home:
- None:
    - aus der Sicht von einem Mitarbeiter -> von Freund Tier ausgeliehen -> hat funktioniert -> deshalb Webseite (Zitat)
    - Bild Mitarbeitern 
    - kurze Erklärung 

- dazischen neue Seite: 
    - Rezensionen/ Bewertungen
    - 

- Primary: 
    - Impressum
        - Support 
        - Verlinkung Navigation
 
SESSION_SECRET auf random string setzen zur sicherheit

views:
    - Ausloggen Button auf Position Login
    - Visibility ändern

Bilder für Tiere 

Kontakt / Partner werden -> Formular -> Ticket into DB

UIKIT -> Bild Upload Button -> umbennen auf TierID -> ausgabe 

//Logout Visibility
app.use(function (req, res, next) {
    res.locals.session = req.session;
    next();
});

        <%if (!locals.session.loggedin) { %>
            <div class="uk-navbar-right uk-text-bold" style="margin-right:50px">
                <ul class="uk-navbar-nav">
                    <li><a href="/login" id="login">Login</a></li>
                </ul>
            </div>
        <% }else{ %>
            <div class="uk-navbar-right uk-text-bold" >
                <form action="/logout?_method=DELETE" method="POST">
                    <button class="uk-button uk-button-default" type="submit" id="logout"> Ausloggen</button>
                </form>
            </div>
        %}%>
